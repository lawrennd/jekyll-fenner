# Fenner fixture site

Golden publication posts for CIP-0003 rendering checks. Site chrome here is
deliberately minimal; Fenner owns the citation stack and is linked in by
`scripts/pilot_build_fixture.sh`.

Path-1 build uses Fenner type layouts (`layout: publication`).
Path-2 build overlays `path2-layouts/` (`layout: default` + `paper_abstract`).
